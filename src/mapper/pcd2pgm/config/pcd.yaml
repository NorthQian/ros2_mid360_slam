/pcd2pgm:
  ros__parameters:
    # 实际路径由 pcd2pgm.launch.py 自动填成 <工作空间>/maps/。
    # 末尾分隔符不能少：节点内部是 file_directory + file_name + ".pcd" 直接拼接。
    # 这里的值只在使用 ros2 run pcd2pgm pcd2pgm_node 直连时作为兜底。
    file_directory: maps/
    file_name: test
    flag_pass_through: false
    map_resolution: 0.05
    map_topic_name: map
    thre_radius: 0.5
    thre_z_max: 0.35
    thre_z_min: -0.04
    thres_point_count: 10
    use_sim_time: false
